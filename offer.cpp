#include "offer.h"
#include <random>
void offer:: set_discount(int d_amount,int d_nums)
{
    discount_amount = d_amount;
    discount_nums = d_nums;
    for(int i=0;i<d_nums;i++)
    {
        //random_string(size_t length);
       // random_string();
    }
}
