#include "comment.h"
#include <iostream>
#include <vector>
using namespace std;
int comment::return_id(){ return id; }
string comment::return_user(){ return user; }
string comment::return_comment(){ return comments; }
